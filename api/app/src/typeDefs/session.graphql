type Session {
	EntityId: ID!
	CreatedDate: DateTime!
	Cost: Int
	AppointmentType: String
	SessionUsed: Boolean
	TrainerPaid: Boolean
	InArrears: Boolean
	ClientId: ID!
}

input SessionDeleteInput {
	sessions: [ID]!
}
