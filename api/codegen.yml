overwrite: true
schema: 'app/src/**/*.graphql'
generates:
    app/src/generated/gen-types.ts:
        config:
            defaultMapper: any
        plugins:
            - typescript
            - typescript-resolvers
