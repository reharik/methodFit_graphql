overwrite: true
schema: 'app/src/**/*.graphql'
generates:
    app/src/generated/gen-types.ts:
        config:
            defaultMapper: any
            contextType: ./#Context
        plugins:
            - typescript
            - typescript-resolvers
